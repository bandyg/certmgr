{
  "name": "cert-manager",
  "version": "1.0.0",
  "description": "Certificate and CA Manager for gRPC mutual TLS",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --import tsx src/index.ts",
    "dev": "node --import tsx --watch src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "log4js": "^6.9.1",
    "uuid": "^9.0.1",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^25.3.3",
    "@types/uuid": "^9.0.7",
    "bun-types": "^1.0.0",
    "tsx": "^4.21.0"
  }
}